let userInput = ''; let operation = ''; let result = ''; let currentUser = null; let tasks = []; function calculateResult(num1, num2, operation) { switch (operation) { case 'add': return num1 + num2; case 'subtract': return num1 - num2; case 'multiply': return num1 * num2; case 'divide': if (num2 !== 0) { return num1 / num2; } else { return 'Error: Division by zero'; } default: return 'Error: Invalid operation'; } } function updateDisplay(result) { const displayArea = document.getElementById('display'); displayArea.innerText = result; } function handleUserInput(input) { if (input === '=' || input === 'Enter') { const num1 = parseFloat(userInput.slice(0, userInput.indexOf(operation))); const num2 = parseFloat(userInput.slice(userInput.indexOf(operation) + 1)); const calculatedResult = calculateResult(num1, num2, operation); updateDisplay(calculatedResult); } else if (input === '+' || input === '-' || input === '*' || input === '/') { operation = input; userInput += input; } else if (input === 'C' || input === 'Clear') { userInput = ''; operation = ''; result = ''; updateDisplay(''); } else { userInput += input; } } function authenticateUser(username, password) { // TO DO: implement user authentication logic, for example, using a database or a file // for demonstration purposes, a simple object is used const users = { 'user1': 'password1', 'user2': 'password2' }; if (users[username] === password) { currentUser = username; return true; } else { return false; } } function addTask(taskDescription) { if (currentUser !== null) { tasks.push({ description: taskDescription, completed: false }); } else { console.log('Please authenticate before adding tasks.'); } } function deleteTask(taskIndex) { if (currentUser !== null && taskIndex >= 0 && taskIndex < tasks.length) { tasks.splice(taskIndex, 1); } else { console.log('Please authenticate before deleting tasks or check the task index.'); } } function markTaskAsCompleted(taskIndex) { if (currentUser !== null && taskIndex >= 0 && taskIndex < tasks.length) { tasks[taskIndex].completed = true; } else { console.log('Please authenticate before marking tasks as completed or check the task index.'); } } document.addEventListener('DOMContentLoaded', function() { const inputButtons = document.querySelectorAll('.button'); inputButtons.forEach(function(button) { button.addEventListener('click', function() { handleUserInput(button.innerText); }); }); const addTaskButton = document.getElementById('add-task-button'); addTaskButton.addEventListener('click', function() { const taskDescription = document.getElementById('task-description').value; addTask(taskDescription); document.getElementById('task-description').value = ''; }); const deleteTaskButton = document.getElementById('delete-task-button'); deleteTaskButton.addEventListener('click', function() { const taskIndex = parseInt(document.getElementById('task-index').value); deleteTask(taskIndex); document.getElementById('task-index').value = ''; }); const markTaskButton = document.getElementById('mark-task-button'); markTaskButton.addEventListener('click', function() { const taskIndex = parseInt(document.getElementById('task-index').value); markTaskAsCompleted(taskIndex); document.getElementById('task-index').value = ''; }); const styleSheet = document.createElement('link'); styleSheet.rel = 'stylesheet'; styleSheet.href = 'style.css'; document.head.appendChild(styleSheet); });